# ---- large data / generated: never commit ----
# PDFs (the graded library — 6+ GB)
*.pdf
**/_pruned/
# vector DB, logs, venv
rag/lancedb/
rag/logs/
# regeneratable control-plane outputs (quarantine_scan.py / rag_control/eval_run.py)
rag/quarantine/
rag/rag_control/eval_report.json
rag/.venv/
.venv/
# python caches
**/__pycache__/
*.pyc
*.pyo
# model weights / adapters / big binaries
lora/
adapters/
index/
chunks/
*.safetensors
*.bin
*.gguf
*.pt
*.npz
# os cruft
.DS_Store

# personal history (health data) — keep local, never push
history.md
rag/history.md
# private HealthCoach user state (candidate ledger and future local-only state)
rag/.healthcoach/

# API keys / secrets — never commit
.env
rag/.env
*.env
